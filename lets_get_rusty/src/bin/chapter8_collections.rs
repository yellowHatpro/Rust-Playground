//Collections:
//These are allocated in heap, and their size can grow and shrink

pub mod chapters;

fn main() {
    //vectors
    chapters::chapter8::vectors::vector();

    //Strings
    chapters::chapter8::strings::strings();

    //Hashmaps
    chapters::chapter8::hashmaps::hashmaps();
}
